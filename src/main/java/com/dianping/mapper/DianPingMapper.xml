<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dianping.dao.DianPingDAO">
<!--设置domain类和数据库中表的字段一一对应，注意数据库字段和domain类中的字段名称不致，此处一定要！ 
	<resultMap id="BaseResultMap" type="com.dianping.model.ProxyIpInfo">
		<id column="ip" property="ip" jdbcType="VARCHAR" />
		<result column="port" property="port" jdbcType="INTEGER" />
	</resultMap>-->

	<!-- 添加单条记录 -->
	<insert id="add" parameterType="com.dianping.model.DianPingInfo" >
		insert
			 into 
		t_shop
			 (id,shopName,average,taste,env,service,address,cookStyle,comment,tag,url,img,createTime,threadNo)
		 values
		    (uuid(),#{shopName},#{average},#{taste},#{env},#{service},#{address},#{cookStyle},#{comment},#{tag},#{url},#{img},now(),#{threadNo})

	</insert>
	
	
	
	
	
	
</mapper>